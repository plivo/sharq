MAJOR RELEASE v0.5.0
	- Adds support for Redis Cluster mode in TCP socket mode only

MAJOR RELEASE v0.3.0
	- Adds the requeue limit feature which bounds
	  the job retries in SHARQ.

MAJOR RELEASE v0.2.0
	- Adds a new interval API to change the queue
	  interval on the fly.

MINOR RELEASE v0.1.1
	- Fixes a bug in metrics queue_id list API.
	- Return failure when a finish is sent to an
	  non existent job.
